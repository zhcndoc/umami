---
title: 概览
---

Umami 允许你通过调用应用程序的 API 端点直接获取数据。  
任何你可以通过应用程序执行的操作在 API 中都能完成。所有数据均以 JSON 格式返回。

### 自托管

自托管时，API 端点可通过 `http://<your-umami-instance>/api` 访问。你需要先[认证](/docs/api/authentication)后才能进行调用。

### Umami 云

如果使用 Umami 云，则通过 `https://api.umami.is` 调用接口，需使用[API 密钥](/docs/cloud/api-key)。